<h1>HI</h1>

<% @events.each do |event| %>
  <p>
    <%= event.name %>
    <%= link_to "Show", :controller => :events, :action => :show, :id => event.id %>
    <%= link_to "New event", :controller => :events, :action => :new %>
    <%= link_to "Edit", :controller => :events, :action => :edit, :id => event.id %>
    <%= link_to "Delete", :controller => :events, :action => :destroy, :id => event.id %>


  </p>

<% end %>
